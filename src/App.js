import React from 'react';
import Layout from './components/Layout';
import Router from './Router';
import './App.css'

const App = () => {
    return (
        <Layout>
            <Router/>
        </Layout>
    );
};

export default App;
